<p>
    This table provides Garbage Collector statistics for the app server's runtime. To get worker stats, visit <a href="/stats/sidekiq">Sidekiq statistics</a> page.
</p>

<table class="table is-striped is-narrow">
    <thead>
        <tr>
            <th>GC Stat</th>
            <th>Value</th>
        </tr>
    </thead>
    <tbody>
        <% GC.stat.each do |stat, value| %>
            <tr>
                <td><%= stat.to_s.gsub('_',' ').capitalize %></td>
                <td><%= value %></td>
            </tr>
        <% end %>
    </tbody>
</table>
